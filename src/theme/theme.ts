'use client ';

import { createTheme } from '@mui/material';

const theme = createTheme({
  palette: {
    primary: {
      main: '#2E183B',
    },
    secondary: {
      main: '#f3e9f1',
    },
  },
});

export default theme;
